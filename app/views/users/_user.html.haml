%tr{ href: user_path(user) }
  %td.center
    %strong= user_counter + 1
  %td
    = gravatar_for user, size: 40
    = link_to user.name, user_transactions_path(user)
  %td= user.product
  %td{ class: "right #{user.net_income > 0 ? 'positive' : 'negative'}" }
    %strong= user.net_income